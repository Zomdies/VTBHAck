import React, { useState, Fragment, useContext } from 'react'
import { Button, Card, Input, TextField, Dialog, DialogTitle, Grid } from '@material-ui/core'

export default function renderStr(props) {

    const { str } = props

    return (
        str
    );
}
